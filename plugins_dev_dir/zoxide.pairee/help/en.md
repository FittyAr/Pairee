# zoxide.pairee — History navigation

Press <F6> to open the zoxide picker (requires `zoxide` on your
`PATH`). The plugin subscribes to `on_cd` so every panel
navigation is automatically added to the zoxide history.
