# fzf.pairee — Fuzzy file navigation

Press <F5> to open the fzf picker against the active panel's
current directory. fzf filters the entries in real time; the
selected path either navigates the panel (if a directory) or is
revealed in the opposite panel (if a file).
