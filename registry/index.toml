schema_version = "1"
generated_at = "2026-07-27T21:50:00Z"

[plugins]

[plugins."archive-inspect.pairee"]
name = "archive-inspect.pairee"
version = "1.0.0"
description = "Preview the contents of ZIP, TAR.GZ and 7Z archives in the file panel without extracting them."
author = "FittyAr"
languages = ["en", "es"]
hooks = ["preview"]
min_pairee = "0.7.0"

[plugins."disk-usage.pairee"]
name = "disk-usage.pairee"
version = "1.0.0"
description = "Show a sorted breakdown of disk usage in the active panel. Find what is eating your space."
author = "FittyAr"
languages = ["en", "es"]
hooks = ["ctrl-d"]
min_pairee = "0.7.0"

[plugins."recent-files.pairee"]
name = "recent-files.pairee"
version = "1.0.0"
description = "Tracks recently visited files and directories in the background and lets you jump back to them with a single keypress."
author = "FittyAr"
languages = ["en", "es"]
hooks = ["on_cd", "on_hover"]
min_pairee = "0.7.0"
