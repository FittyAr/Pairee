# Lua API v1.0.0

Stability: **Stable** unless marked *Preview*. Removing or renaming a Stable
entry is a **MAJOR** bump.

## Runtime

| Binding | Kind | Notes |
|---------|------|--------|
| `pairee._lua_api_version` | string | `"1.0.0"` |
| `pairee._secure_mode` | bool | Host secure mode |
| `pairee.settings` | table | Manifest defaults + user overrides |
| `pairee.t(key, vars?)` | function | Plugin i18n |
| `pairee.sync(fn)` | async | Snapshot + fill `pairee.cx`, then call `fn` |

## Dialogs and notify

| Binding | Returns | Notes |
|---------|---------|--------|
| `pairee.confirm({pos?, title, body})` | boolean | TUI; Y/S/Enter or N/Esc |
| `pairee.input({pos?, title, value?, obscure?, realtime?, debounce?})` | `{value, event}` | event 1=submit, 2=cancel |
| `pairee.which({cands, silent?})` | integer? | 1-based index or `nil` |
| `pairee.notify({title, content, level?, timeout?})` | — | Structured notify |
| `pairee.app.confirm(title, msg)` | boolean | Deprecated; same TUI |
| `pairee.app.input(title, default)` | string | Deprecated; same TUI |

## Context (`pairee.cx`)

Filled on each `pairee.sync`. Before the first sync, tables exist with empty cwd.

| Path | Type |
|------|------|
| `cx.active.cwd` | string |
| `cx.active.hovered` | `File` or `nil` |
| `cx.active.selected` | `File[]` |
| `cx.active.current.cwd` / `.hovered` | same as active |
| `cx.left.cwd` / `cx.right.cwd` | string |

`File` fields: `name`, `path`, `url`, `size`, `is_dir`, `is_symlink`.  
`tostring(file)` is `path`. `file == other` compares paths.

## Filesystem (`pairee.fs`)

Throwing on error (same as 0.7). Paths are strings or `File`.

| Binding | Notes |
|---------|--------|
| `read` / `write` / `exists` | Existing |
| `stat` / `file` | `File` userdata (`stat` is `nil` if missing) |
| `list` / `read_dir` | `File[]` |
| `mkdir(kind, path)` | `"dir"` or `"dir_all"` |
| `remove(kind, path)` | `"file"`, `"dir"`, `"dir_all"`, `"dir_clean"` |
| `rename(from, to)` | |
| `copy(from, to)` | returns bytes copied |
| `spawn(cmd, args)` | trusted only; prefer `Command` |
| `spawn_copy_task(from, to)` | Transfer Engine job |

On the multi-thread plugin worker, I/O uses `tokio::fs`.

## `pairee.Command` (trusted)

```lua
pairee.Command(prog):arg(s|list):cwd(path):env(k,v)
    :stdin(PIPED|NULL|INHERIT):stdout(...):stderr(...)
    :spawn() | :output() | :status()
```

`Child`: `id`, `write_all`, `read`, `read_line`, `close_stdin`, `wait`,
`wait_with_output`, `try_wait`, `start_kill`.

Untrusted plugins cannot spawn. Secure Mode applies the command blacklist.

## App / emit / utils / UI

| Binding | Notes |
|---------|--------|
| `pairee.app.cwd/cd/focus/set_focus/notify/hovered` | Legacy app table |
| `pairee.emit(action, args)` | `cd`, `set_focus` / `focus` |
| `pairee.file_cache({file, skip})` | Preview cache path |
| `pairee.utils.target_os/family/time/hash/quote` | |
| `pairee.utils.percent_encode/decode` | |
| `pairee.utils.json_encode/decode` | *Preview* (async) |
| `pairee.utils.sleep(secs)` | *Preview* (async) |
| `pairee.ui.*` | Table widget stubs |
| `pairee.ps.sub/pub/unsub` | Local pub/sub |
| `pairee.log.info/warn/error/debug` | Host log |

## Preview (not in 1.0)

`Url`/`Cha`/`Error` userdata, `pairee.image.*`, full ratatui widgets, `rt`/`th`/`km`.
See [plugin-roadmap.md](../../technical/plugin-roadmap.md).
