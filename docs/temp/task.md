# Tasks — Plugin System Implementation

- [x] Add `mlua` dependency in `Cargo.toml`
- [x] Add plugin structures to `Settings` and `PopupType`
- [ ] Create core plugin engine modules (`src/plugin/`)
    - [ ] `mod.rs` and module skeleton
    - [ ] `manager.rs` (discovery and lifecycle)
    - [ ] `loader.rs` (loading and version checks)
    - [ ] `registry.rs` (loaded index & keybindings)
    - [ ] `hooks.rs` (HookBus events)
    - [ ] `sandbox.rs` (safe StdLib & Secure Mode process blacklist)
    - [ ] `runtime/` bindings and Lua type structures
    - [ ] `updater.rs` (CLI downloader and verification)
    - [ ] `developer_tool.rs` (formatter, linter, PR submitter)
- [ ] Integrate plugin manager into CLI and Main Event Loop
    - [ ] CLI command routing (`pairee plugin`, `pairee developer`)
    - [ ] Sync request processing in event loop
    - [ ] Dispatch event emitters (`on_cd`, `on_hover`, `on_key`)
- [ ] Implement UI views for plugins
    - [ ] Previewer rendering (`draw_quick_view` plugin widgets)
    - [ ] Help menu tab integration (`F1` screen)
- [ ] Run verification tests
